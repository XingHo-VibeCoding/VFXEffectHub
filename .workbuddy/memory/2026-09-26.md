# 2026-09-26（Day 9 · 周六）

## 今日任务清单（用户发来）
Day 9｜第 2 周——板块顺序：① 了解设计规则 ② 用规则做前端审查 ③ 修复
- 主任务：用设计规则完成一次前端审查和修复
- 今日不做：大改版面结构；新增页面
- 完成标准：至少修掉 3 处设计问题（文字对比、间距、对齐等）；修前后截图各一张
- 截图要求：修复前、后两张，含地址栏，能看出同一位置的对比度/间距/对齐变化
- 每日一问（收尾时问用户）：今天改的几处里，哪一处对「像不像一个正经产品」影响最大？
- 必须不变的功能：四状态、tab 切换、搜索/筛选、卡片点击进详情

## 板块 ① 了解设计规则（已讲，纯讲解无文件改动）
讲了六条带数字阈值的设计规则：
1. 文字对比度：正文 ≥ 4.5:1，大标题 ≥ 3:1（WCAG）
2. 间距系统：4px 基数、8px 节奏，同类间距全站一致
3. 对齐：所有区块共用同一条左边线（container 统一宽度与 padding）
4. 字号层级：阶梯清晰、字重区分，不出现连续字号混用（12/13/14）
5. 按钮状态：hover / active / focus / disabled 四态齐全
6. 移动端：无横向溢出、触控目标 ≥ 44px

## 板块 ②③ 待办（等「进入下一板块」）
- ③收尾：用户截「修复后」图对比；收尾 commit + push；每日一问

## 板块 ② 审查结果（用户拍板：5 处全修）
1. 文字对比度：#8b949e（~3:1）多处小字不达标
2. .btn 定义两次（Day 7 可见边框 vs Day 8 透明边框，后者覆盖前者）——审查时还漏了 .btn--primary 也重复定义
3. 按钮/输入框触控目标 ~37px < 44px
4. .tab/.btn 无 focus-visible 键盘聚焦态
5. 工具栏 gap 8 vs 12 不一致；辅助字号 11/12/13 混用
- 不修（如实说明）：分区页与首页两套卡片类名统一属「大改版面结构」，今日清单明确不做

## 板块 ③ 修复（全部完成，仅改 css/style.css，HTML/JS 零改动）
1. 对比度：`sed 's/#8b949e/#57606a/g'` 全站 7 处替换（约 7:1）
2. 按钮合并：删 Day 7 的 .btn/.btn:hover/.btn--primary/.btn--primary:hover（保留详情页专用 .btn--active），Day 8 .btn 补全为可见边框（#d0d7de）+ hover/active 四态齐全；全站只剩一套 .btn（grep 验证 `^\.btn {` 仅 1 处）
3. 触控目标：@media 560px 内 .toolbar__search/.toolbar__select/.btn 加 min-height:44px，.tab padding 加高到 14px
4. focus 态：文件末尾统一 :focus-visible 规则（2px #1f6feb 外框 + offset 2px），覆盖 tab/btn/搜索框/下拉
5. 间距字号：.toolbar gap 8→12（与首页统一）；.chip 11→12、.detail-hint/.tag-chip/.collection-link-hint/.collection-item-count 13→12；admin 页 13px 表单文字保留（内部工具页不在审查范围）；13px 动态消息类（collection-msg/fav-msg）保留为反馈层级
- 验证手段：grep 确认无 11px、无 #8b949e、.btn 唯一

## 用户验证结果（板块 ③ 通过）
- 首页：页脚/作者名变清晰、工具栏间距变宽、chip 变大——用户截图确认
- 详情页：点赞/收藏按钮恢复可见边框——用户截图确认
- 验证过程踩坑：①用户开了 PowerShell 而非 Git Bash（命令语法不同，给了 PowerShell 版命令 + Git Bash 打开方法）；②Git Bash 不支持 Ctrl+V，用右键→Paste 或 Shift+Insert；③浏览器缓存需 Ctrl+Shift+R 强刷
- **每日一问答案：用户选 A（对比度）——小字不发虚、一眼可读，对「像不像正经产品」影响最大**

## 收尾（待 commit + push）
- 改动文件：css/style.css（唯一代码文件，40+/36−）+ .workbuddy/memory/2026-09-23.md、MEMORY.md（Day 8 补记）+ 2026-09-26.md（今日日志）
- commit 计划：`Day 9｜设计规则审查：修掉5处视觉问题`
- 候选问题（初查 CSS 已发现，待板块②确认）：
  - .card-author / .empty-hint / .site-footer 用 #8b949e（白底对比度约 3:1，小字号不达标 4.5:1）
  - .toolbar select 与 .toolbar__select 两套选择器样式重复且不一致（间距对齐隐患）
  - .chip 标签 #57606a on #f0f1f3 对比度临界
  - 移动端 .toolbar__select 下拉与按钮高度/触控目标
  - 分区页 .card 与首页 .card 两套卡片类名并存（card-thumb vs card__cover），间距不统一
