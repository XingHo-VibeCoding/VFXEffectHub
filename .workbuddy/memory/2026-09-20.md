# 2026-09-20 工作日志（Day 5）

## 今日进度
- 板块 ① 前后端与数据库分工：完成讲解（配两张图：通用三分工示意 + VFXEffectHub 功能分工表）；核心结论——判断标准是「数据是否需要多人共享写入」，我们的 MVP = 前端 + Git 仓库 + localStorage，不需要后端；砍掉的登录/统计/云同步恰好就是需要后端的三个，需求与架构咬合
- 板块 ② 技术路线：用户三项全按推荐拍板——纯 HTML/CSS/JS、GitHub Pages、收录工具页；数据格式直接定 JSON（works.json）
- docs/TECH_DESIGN.md 初版落盘：架构总览、技术路线表（含理由与落选原因）、页面目录结构、works.json 字段设计、防盗链应对、本地开发/部署、风险限制；第 7 节数据流图留空待板块 ③ 填入

## 技术要点（Day 7 开发要用）
- localStorage 键名约定：vfx-likes / vfx-favs / vfx-collections
- fetch JSON 必须经 http://（本地起 python -m http.server 8000，不能 file:// 双击）
- no-referrer 用 <meta name="referrer" content="no-referrer">，每个页面 head 都要加
- 三个分区页共用 common.js，按 sourceType 过滤同一份 works.json

## 待办
- ~~板块 ③ 数据流图~~ 已完成：Mermaid 版数据流图（收录流 + 浏览流）已填入 TECH_DESIGN.md，含「一句话」答案
- ~~收尾~~ commit a648464 已落，等用户 push + 截图确认
- 余力加练（可选）：方案变化影响表、技术栈科普

## 追加：按课程模板补强 TECH_DESIGN.md（用户确认后执行）
- 用户贴了课程通用模板（占位符未填），对照后发现现有文档缺三项：API 列表、错误处理、环境变量
- 已补三节并顺延节号（现共 12 节）：§5 数据接口清单（纯前端的「API」：fetch works.json / img 加载 / localStorage 读写 / 工具页生成 JSON）、§6 错误处理（7 种场景逐条定处理方式，原则「不白屏」）、§7 环境变量（写明「无」及理由，与 .gitignore 的 .env 规则呼应）
- 占位符未按「先列问题」规矩直接编造——先列了 4 个疑问 + 差距表，用户拍板走「补强现有文档」
- 用户确认 3 个占位符推测全对 → 顺势补全 §4：localStorage 三键的完整数据结构（vfx-likes/vfx-favs 纯 id 数组；vfx-collections 每个收藏夹含 id/name/workIds/createdAt 四字段）；明确「收藏夹装作品 id 而非拷贝，展示时过滤失效 id」
- 提交前发现 TECH_DESIGN.md 有 43 行纯表格格式化的未提交改动（编辑器自动美化留下），用户拍板还原 → 工作区干净再推送
- 用户追问「账号算数据对象吗」→ 概念讲解：账号是数据对象，我们没有是因为登录已砍；localStorage 是「本机 = 本人」的替代方案
- 用户追问「有线上数据库持久化说明吗」→ 答：散落多处无统一节；补 §13 持久化说明（持久化责任分工 + 与传统系统对比表 + 弱点升级方向）
- 用户追问「线上体现在哪呢」→ 实测：GitHub 仓库已 Public（https://github.com/XingHo-VibeCoding/VFXEffectHub）、GitHub Pages URL 404 未启用；§13 补「当前线上状态验证表」+ 两类 URL 辨析
- 用户提交 Day 5 后半段清单（项目结构对应、待决定事项、完成后检查、每日一问）→ 补两节：§14 数据对象×页面×接口 映射表（验收 1+2 的「指着某张表说出」+「每个动作有归宿」）；§15 待决定事项 TBD-1~TBD-7（Day 7 起逐项拍板）
